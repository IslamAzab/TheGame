%ul
  - versions = result.versions
  - versions.each do |version|
    / every version holds the state before entering this version, so to get the state 	during this version we have to fetch it from next version, in case of the last 		version, the state is the result state (live state)
    - attrs = PaperTrail.serializer.load(versions[1+version.index].try(:object) ||"") || result
    
    %li#left-align="#{version.whodunnit} #{version.event}d the result with score #{attrs['score']} and comment #{attrs['comment']}"